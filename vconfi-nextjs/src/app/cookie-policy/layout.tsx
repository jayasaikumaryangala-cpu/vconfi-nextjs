import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Cookie Policy', description: 'VCONFI Cookie Policy — how we use cookies on our website. Enterprise IT services, Hyderabad.', alternates: { canonical: 'https://vconfi.com/cookie-policy' } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
